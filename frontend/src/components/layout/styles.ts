import { createStyleSheet } from 'src/utils/theme/createStyleSheet';

export const styles = createStyleSheet({
  siderWrapper: {
    background: '#fff',
    height: '100%',
  },
  navbarWrapper: {
    background: '#f2f2f2',
  },
  mainWrapper: {
    flex: 1,
    background: '#f1f1f1',
    overflow: 'scroll'
  },
});
